# DInject with Maven and Kotlin KAPT

Note that Kotlin KAPT does not generate the source automatically 
(on kotlin compile) so instead we need to go:

Idea / Maven / Right mouse -> Generate sources and update folders

